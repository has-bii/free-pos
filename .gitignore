node_modules
dist
.turbo
.wrangler
.dev.vars
*.log
.DS_Store
.env
.env.*
!.env.example
# `!.env.example` matches that exact name only — each additional committed
# example file needs its own negation.
!.env.test.example
